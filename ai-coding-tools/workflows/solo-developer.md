# Solo Developer Workflow

One IDE agent (**Cline** BYOK or **Cursor** if subscription-fits) + one
terminal agent (**Aider**/**Claude Code**) + a rules file in every repo +
MCP for your database/stack. Work in small tasks, commit checkpoints, let
the terminal agent handle chores (renames, dependency bumps, migrations)
while you keep architecture decisions.
